enum OS {
    Android = "android",
    IOS = "ios",
    Web = "web",
    Windows = "windows",
    MacOS = "macos",
}

enum ColorMode {
    Light = "light",
    Dark = "dark",
}

export { OS, ColorMode }

{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    /* 
      在这里添加的选项会覆盖 "extends" 中的同名选项。
      我们需要覆盖 "lib" 选项，在 React Native 的基础上
      额外添加 "dom" 类型库，以支持 Web 环境。
    */
    "lib": ["esnext", "dom"]
  }
}